<?php
require('conexao.php');

$id_cont = filter_input(type:INPUT_GET, var_name: 'id_cont', filter: FILTER_DEFAULT);

 // filter_input pega o valor do id_cont 

$sql = "SELECT * FROM `continentes` WHERE id_cont = :id";
$statement = $pdo->prepare(query: $sql);

//  o prepare é usado para preparar a consulta SQL, e o execute é usado para executar a consulta com os parâmetros fornecidos.

$statement->execute([':id' => $id_cont]);
$result = $statement->fetch(mode: PDO::FETCH_ASSOC);

 // diferente do fetchAll, o fetch busca apenas um resultado.
 
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>


<div class="container"> 
    <div class="container-filho">
  
    <button><a href="continentes.php"> Voltar</a></button>
</div>
</div>
    
</body>
</html>